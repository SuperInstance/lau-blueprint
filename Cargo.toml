[package]
name = "lau-blueprint"
version = "0.1.0"
edition = "2021"
description = "Blueprint/building plan system for kids to design structures"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
